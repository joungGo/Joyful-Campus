<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/alarm"
        android:icon="@drawable/alarm_icon"
        android:title="알림"
        app:showAsAction="always|withText" />

    <item
        android:id="@+id/find"
        android:icon="@drawable/find_iconsvg"
        android:title="검색"
        app:showAsAction="always|withText" />

    <item
        android:id="@+id/clubToolbarBookmark"
        android:icon="@drawable/bookmark_icon"
        android:title="북마크"
        app:showAsAction="always|withText" />

</menu>
